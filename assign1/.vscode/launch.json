{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/imdbtest",
            "args": [
                "Meryl Streep"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}